[tool.uv]
exclude-newer = "1 day"

[tool.pyfltr]
preset = "latest"
dotnet = true
js-runner = "pnpm"
prettier = true
colloquial-check = true

extend-exclude = [
    "bin",
    "obj",
    "publish",
    "node_modules",
]
