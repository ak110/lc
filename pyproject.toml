[tool.uv]
exclude-newer = "1 day"

[tool.pyfltr]
preset = "latest"
python = false
js-runner = "pnpm"
dotnet-format = true
dotnet-build = true
dotnet-test = true
prettier = true

extend-exclude = [
    "bin",
    "obj",
    "publish",
    "node_modules",
]
